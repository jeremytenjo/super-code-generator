import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import Avatar from './'

<Meta title="Avatar" component={Avatar} argTypes={{}} args={{ test: 'test' }} />

# Avatar

export const Template = (args) => <Avatar {...args} />

<Canvas>
  <Story name="default" argTypes={{}} args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
